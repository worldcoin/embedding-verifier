[package]
name = "di-types"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.0"
license = "MIT"
publish = false

# In the di-enclave graph, so nothing here is inherited from the root workspace.
# See the note in the root Cargo.toml.
[dependencies]
enclave-types = { path = "../../shared/enclave-types" }
