# Build artifacts and VCS metadata — never needed in the image build context.
target/
**/target/
build/
.git/
.github/

# Private model artifacts are fetched into the Nix store by scripts/build-enclaves.sh.
models/

# Local editor / OS cruft.
.vscode/
.idea/
.DS_Store
