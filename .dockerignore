# Build artifacts and VCS metadata — never needed in the image build context.
target/
**/target/
build/
.git/
.github/

# Private model artifacts are downloaded inside the model-downloader stage.
models/

# Local editor / OS cruft.
.vscode/
.idea/
.DS_Store
