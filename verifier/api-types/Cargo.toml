[package]
name = "flamingo-verifier-api-types"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# Host-side only, and deliberately not in either enclave's source set: the HTTP contract stops
# at the host, so none of it belongs in an EIF.
[dependencies]
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true
